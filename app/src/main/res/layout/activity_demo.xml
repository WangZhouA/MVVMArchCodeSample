<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="com.goldze.mvvmhabit.ui.main.DemoViewModel" />

        <variable
            name="viewModel"
            type="DemoViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#202020"
        android:orientation="vertical">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="网络访问"
            onClickCommand="@{viewModel.netWorkClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="RecycleView多布局"
            onClickCommand="@{viewModel.rvMultiClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="底部Tab按钮"
            onClickCommand="@{viewModel.startTabBarClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ViewPager绑定"
            onClickCommand="@{viewModel.viewPagerBindingClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ViewPager+Fragment"
            onClickCommand="@{viewModel.viewPagerGroupBindingClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="表单提交"
            onClickCommand="@{viewModel.formSbmClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="表单编辑"
            onClickCommand="@{viewModel.formModifyClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="权限申请"
            onClickCommand="@{viewModel.permissionsClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="全局异常捕获"
            onClickCommand="@{viewModel.exceptionClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="文件下载"
            onClickCommand="@{viewModel.fileDownLoadClick}" />
    </LinearLayout>
</layout>